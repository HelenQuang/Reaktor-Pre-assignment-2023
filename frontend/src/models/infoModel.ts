interface infoModel {
  _id: string;
  droneSerialNumber: string;
  distance: number;
  pilotName: string;
  pilotEmail: string;
  pilotPhoneNumber: string;
  observedAt: Date;
}

export default infoModel;
